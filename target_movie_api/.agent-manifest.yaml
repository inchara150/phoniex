version: "1.0"
project:
  name: "movie-recommendation-backend"
  language: "python"
  framework: "flask"
sandbox:
  base_image: "python:3.11-slim"
  install_command: "pip install sqlalchemy pytest flask" # <-- Added flask here
verification:
  test_command: "python -m pytest tests/"
database:
  driver: "sqlite"