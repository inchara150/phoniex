version: "1.0"
project:
  name: "demo-service"
  language: "python"
  framework: "flask"
sandbox:
  base_image: "python:3.11-slim"
  install_command: "pip install pytest"
verification:
  test_command: "python -m pytest tests/"
database:
  driver: "sqlite"